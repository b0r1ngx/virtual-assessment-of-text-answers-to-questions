CREATE TABLE IF NOT EXISTS UserType (
  id    INTEGER PRIMARY KEY,
  name  TEXT NOT NULL UNIQUE
);

INSERT OR IGNORE INTO UserType(name) VALUES ('Student');
INSERT OR IGNORE INTO UserType(name) VALUES ('Teacher');

selectAll:
SELECT * FROM UserType;
