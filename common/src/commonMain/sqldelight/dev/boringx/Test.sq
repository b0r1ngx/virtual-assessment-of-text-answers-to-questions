CREATE TABLE Test(
  id          INTEGER PRIMARY KEY,
  creator_id  INTEGER,
  course_id   INTEGER,
  name        TEXT,
  start_at    TEXT,
  end_at      TEXT,
  created_at  TEXT,
-- _at holdings DATE as ISO8601 strings ("YYYY-MM-DD HH:MM:SS.SSS")

  FOREIGN KEY(creator_id) REFERENCES User(id),
  FOREIGN KEY(course_id) REFERENCES Course(id)
);

selectAll:
SELECT * FROM Test;

selectBy:
SELECT * FROM Test
WHERE course_id = ?;

insert:
INSERT INTO Test(creator_id, course_id, name)
VALUES(?, ?, ?);
