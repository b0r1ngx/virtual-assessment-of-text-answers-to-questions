CREATE TABLE UserType (
  id    INTEGER PRIMARY KEY,
  name  TEXT
);

-- fill it with two types: Student, Teacher
-- to be convenient with Kotlin enum UserType, Student must be first entry of this table